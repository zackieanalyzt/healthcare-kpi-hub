export function KpiPage({ pageId }: { pageId: string }) {
  return (
    <section>
      <h1>KPI Page</h1>
      <p>Placeholder detail route for page `{pageId}`. Read-only page detail wiring stays deferred.</p>
    </section>
  );
}
